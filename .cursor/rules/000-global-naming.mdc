---
description: 適用於所有 Angular 開發物件與檔案的現代化命名規範與基礎 TypeScript 編碼標準
globs: "apps/frontend/src/**/*"
alwaysApply: false
---

# Angular 全域命名與 TypeScript 編碼規範

## 1. 檔案與核心物件命名 (Artifacts)

### 1.1 元件 (Components)
- **檔案命名**: 採用 `kebab-case` 搭配 `.component.ts` 後綴（例：`user-profile.component.ts`）
- **類別命名**: 採用 `PascalCase` 搭配 `Component` 結尾（例：`export class UserProfileComponent {}`）
- **選擇器 (Selector)**: 必須使用 `kebab-case` 且帶有前綴 `app-`（例：`app-user-profile`）
- **模板檔**: 同名 `.html`（例：`user-profile.html`）
- **樣式檔**: 同名 `.css`（例：`user-profile.css`）

### 1.2 服務 (Services)
- **檔案命名**: `kebab-case` 搭配 `.service.ts`（例：`auth.service.ts`）
- **類別命名**: `PascalCase` 搭配 `Service` 結尾（例：`export class AuthService {}`）
- **Registration**: 預設 `providedIn: 'root'`，僅在 lazy feature boundary 時例外

### 1.3 指令 (Directives)
- **檔案命名**: `kebab-case` 搭配 `.directive.ts`（例：`auto-focus.directive.ts`）
- **類別命名**: `PascalCase` 搭配 `Directive` 結尾
- **選擇器**: 屬性選擇器使用 `lowerCamelCase`（例：`[appAutoFocus]`）；元素選擇器使用 `app-` 前綴

### 1.4 管道 (Pipes)
- **檔案命名**: `kebab-case` 搭配 `.pipe.ts`（例：`format-number.pipe.ts`）
- **類別命名**: `PascalCase` 搭配 `Pipe` 結尾
- **name 屬性**: `camelCase`（例：`name: 'formatNumber'`）

### 1.5 注入令牌 (Injection Tokens)
- **命名**: 全大寫蛇形 `CAPS_SNAKE`（例：`DATE_FORMAT`、`API_BASE_URL`）
- **型別**: 必須提供 generic 型別（例：`new InjectionToken<DateFormat>('DATE_FORMAT')`）

### 1.6 介面與型別 (Interfaces & Types)
- **命名**: `PascalCase`，介面以描述性名詞命名，**不**加 `I` 前綴（例：`UserProfile`、`AuthState`）
- **類型別名**: `PascalCase`（例：`type UserId = string`）
- **Enum**: `PascalCase`，成員使用 `PascalCase`（例：`LoadingState.Pending`）

---

## 2. TypeScript 基礎編碼標準

### 2.1 型別安全
- **啟用嚴格模式**: `strict: true` in `tsconfig.json`
- **禁止 `any`**: 使用 `unknown` 處理不確定型別，再透過型別守衛縮窄
- **優先型別推斷**: 當型別顯而易見時，無需顯式標註
- **所有函式**必須標註回傳型別，除非可被明確推斷

### 2.2 存取修飾符
| 場景 | 修飾符 |
|------|--------|
| 供模板使用的屬性/方法 | `protected` |
| 僅供類別內部使用 | `private` |
| 對外公開 API | `public` |
| 不會被重新賦值 | 加上 `readonly` |

### 2.3 依賴注入
- **禁止使用建構子注入**: 改用 `inject()` 函式（例：`private readonly authService = inject(AuthService)`）
- **Injection Token**: 使用 `CAPS_SNAKE` 命名，並提供 factory default

### 2.4 程式碼品質
- **方法長度**: 每個方法 ≤ 50 行，超過則拆分純函式
- **單一職責**: 每個類別/函式只負責一件事
- **Production-ready**: 第一次迭代即寫出可上線的程式碼，**禁止** placeholder 或 mock 實作
- **JSDoc**: 為複雜函式撰寫文件；對複雜業務邏輯加上行內註解
- **禁止 TODO 殘留**: 功能必須完整實作，不留 TODO 於程式碼中
