---
description: 適用於前端與 TypeScript 開發環境的現代化命名規範與基礎 TypeScript 編碼標準
globs:
  - "ClientApp/src/**/*"
  - "apps/frontend/src/**/*"
alwaysApply: false
---

# TypeScript 全域基礎編碼與命名標準

## 1. 核心型別與物件命名規範

### 1.1 介面與型別 (Interfaces & Types)
- **命名**: `PascalCase`，介面以描述性名詞命名，**嚴禁**加 `I` 前綴（例：`UserProfile`、`AuthState`）。
- **類型別名 (Type Aliases)**: `PascalCase`（例：`type UserId = string`）。
- **列舉 (Enums)**: `PascalCase`，成員亦使用 `PascalCase`（例：`LoadingState.Pending`）。

### 1.2 注入令牌 (Injection Tokens)
- **命名**: 全大寫蛇形 `CAPS_SNAKE`（例：`DATE_FORMAT`、`API_BASE_URL`）。
- **型別**: 必須提供泛型標註與 default factory（例：`new InjectionToken<DateFormat>('DATE_FORMAT')`）。

### 1.3 檔案命名基礎
- 預設採用 `kebab-case` 進行檔案與目錄命名。
- 依據職責加上明確後綴（例：`.utils.ts`、`.types.ts`、`.config.ts`）。

---

## 2. TypeScript 基礎編碼標準

### 2.1 型別安全
- **啟用嚴格模式**: 保持 `strict: true` in `tsconfig.json`。
- **禁止 `any`**: 遇到不確定型別時，一律使用 `unknown`，再透過型別守衛 (Type Guards) 縮窄範圍。
- **優先型別推斷**: 當變數或常數的型別顯而易見時，無需顯式標註。
- **所有函式**必須標註回傳型別，除非可被明確且單一地推斷。

### 2.2 存取修飾符
| 場景 | 修飾符 |
|------|--------|
| 供模板或子類別使用的屬性/方法 | `protected` |
| 僅供類別內部使用 | `private` |
| 對外公開 API | `public` |
| 不會被重新賦值 | 加上 `readonly` |

### 2.3 程式碼品質與防禦性設計
- **方法長度**: 每個方法 ≤ 50 行，超過則重構拆分為純函式 (Pure Functions)。
- **單一職責**: 每個類別、函式與模組只負責單一職責。
- **Production-ready**: 第一次迭代即產出高標準、可上線的程式碼，**嚴禁** placeholder 或 mock 實作。
- **文件與註解**: 為複雜演算法與公共 API 撰寫 JSDoc；對特殊商業邏輯補上清晰的行內註解。
- **禁止 TODO 殘留**: 功能必須完整實作，不留任何 TODO 或未完成標記於正式提交中。

